#include<stdio.h>

int my_abs(int x){
	result = x >> 31;
	result = (result+x) ^ result
	return x;
}

void main(){
	int x = 1;
	int y = -1;
	int z = 0;
	printf("%d %d %d\n", my_abs(x), my_abs(y), my_abs(z));
	return;
}
